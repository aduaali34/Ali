# Intentional Students Network (ISN)

This repository was scaffolded to build a Next.js 15 App Router application for the Intentional Students Network.

Run locally:

1. Copy .env.example to .env.local and fill the values.
2. Install dependencies:

   pnpm install

3. Run the dev server:

   pnpm dev

Environment variables are documented in .env.example.
